---
description: SCSS 样式编写规范
globs: src/**/*.scss
alwaysApply: false
---

# 样式规范

## SCSS

- 使用 SCSS 嵌套语法组织样式
- 页面容器宽度统一 `1200px`，居中 `margin: 0 auto`
- 使用 `box-sizing: border-box`
- 卡片样式统一: `border-radius: 8px` + `box-shadow`

## 命名

- 使用 BEM 风格或 kebab-case 类名
- 页面根元素类名与目录名一致（如 `.home`, `.message-board`）

## 响应式

- 固定宽度 1200px 布局，后续可改为响应式
- 导航栏固定顶部 `position: fixed`
